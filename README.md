# knowledge-explorer

An AI Edge Gallery JavaScript skill scaffold for rich topic exploration.

## Structure

- `SKILL.md` — skill metadata and model instructions
- `scripts/index.html` — hidden `run_js` runner
- `assets/webview.html` — inline webview UI

## Current state

Version 1 scaffold now supports a structured query schema with modes for overview, gallery, timeline, audio, compare, and study.